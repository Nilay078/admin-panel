export enum CommonAuthEnum {
    accessToken = 'accessToken',
    refreshToken = 'refreshToken',
}

export enum RoleEnum {
    MASTER_ADMIN = 1,
    SCHOOL_ADMIN = 2,
    ALUMNI = 3,
}

export enum PaginationTypeEnum {
    card = 'card',
    table = 'table',
}
